import XCTest
@testable import fullstory_swiftui_plugin

final class fullstory_swiftui_pluginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(fullstory_swiftui_plugin().text, "Hello, World!")
    }
}
